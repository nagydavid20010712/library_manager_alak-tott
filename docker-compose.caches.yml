version: "3.8"

services:

  redis:
    image: redis:alpine
    command: redis-server --appendonly yes
    env_file:
        - "./caches/.env"
    networks:
        - laravel

  memcached:
    image: docker.io/bitnami/memcached:1
    env_file:
        - "./caches/.env"
    #ports:
    #  - 11211:11211
    networks:
        - laravel

networks:
  laravel:
    driver: bridge